apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
name: constraints-rust
namespace: default
spec:
runLatest:
  configuration:
  revisionTemplate:
    spec:
    container:
      image: gcr.io/finside/option_price_faas:1.0.0
      #env:
      #  - name: TARGET
      #value: "Rust Sample v1"